font-family = Iosevka NFM

# OS-specific overrides (font size, keybinds, etc.)
config-file = ?config-linux
config-file = ?config-macos

# Explicit baseline so the same shape/blink shows up regardless of what's
# rendering into this surface - a plain SSH session (no shell-integration,
# no tmux cursor-style override) otherwise falls back to Ghostty's own
# default, which is a blinking block, not a bar.
cursor-style = bar
cursor-style-blink = true

window-decoration = true
resize-overlay = never

shell-integration = detect
shell-integration-features = ssh-env,ssh-terminfo

# Quick terminal (Quake-style drop-down). global: requires Accessibility
# permission on macOS; on Linux needs a compositor supporting wlr-layer-shell
# (KDE Plasma, wlroots compositors - not GNOME) plus the XDG Global Shortcuts
# portal for the keybind itself to fire outside of Ghostty.
keybind = global:cmd+backspace=toggle_quick_terminal
quick-terminal-position = top
quick-terminal-size = 40%
quick-terminal-animation-duration = 0.2
